import os
def rename_file():
  file_list = os.listdir("C:\OOP\prant")
  
  for file_name in file_list: 
    os.rename(file_name, file_name.translate(None, "0123456789"))
    #string.translate(table, list of characters to remove)
